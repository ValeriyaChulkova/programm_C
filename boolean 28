//Cамостоятельна работа 2 базовый вариант boolean 28
#include <iostream>

using namespace std;

int main() {
    float x, y;
    cout << "Р’РІРµРґРёС‚Рµ РєРѕРѕСЂРґРёРЅР°С‚С‹ С‚РѕС‡РєРё (x, y): ";
    cin >> x >> y;
    if ((x > 0 && y > 0) || (x < 0 && y < 0)) {
        cout << "РўРѕС‡РєР° Р»РµР¶РёС‚ РІ РїРµСЂРІРѕР№ РёР»Рё С‚СЂРµС‚СЊРµР№ РєРѕРѕСЂРґРёРЅР°С‚РЅРѕР№ С‡РµС‚РІРµСЂС‚Рё";
    } else 
    {
        cout << "РўРѕС‡РєР° РЅРµ Р»РµР¶РёС‚ РІ РїРµСЂРІРѕР№ РёР»Рё С‚СЂРµС‚СЊРµР№ РєРѕРѕСЂРґРёРЅР°С‚РЅРѕР№ С‡РµС‚РІРµСЂС‚Рё";
    }
return 0;
}
